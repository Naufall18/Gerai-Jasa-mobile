<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch background shown while the Flutter engine boots.
     Solid brand color so the user never sees a white/black flash before the
     in-app animated splash takes over. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/gj_splash" />
</layer-list>
